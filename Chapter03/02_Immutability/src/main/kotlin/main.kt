import kotlinx.cinterop.IntVar
import kotlinx.cinterop.nativeHeap

fun main(args: Array<String>) {
    val placement = nativeHeap.alloc(1, 0)


    // Allocating some native memory:
//    val myInteger = placement.alloce
}
